package dkeep.logic;

public class Rookie extends Guard{

    /**
     * Constructs a Rookie Guard in the given position, represented internally by the given char
     * @param line: guard's line
     * @param column: guard's column
     * @param charc: guard's representative char
     */
    public Rookie(int line, int column, char charc) {
        super(line, column, charc);
    }


}
